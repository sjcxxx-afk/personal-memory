# Core MCP and database dependencies
mcp>=1.0.0
chromadb>=0.4.0
pydantic>=2.0.0

# Optional: for embedding models
sentence-transformers>=2.2.0
# or
# openai>=1.0.0  # if using OpenAI embeddings

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# CLI interface (optional for first phase)
click>=8.0.0

# Web interface (optional for second phase)
flask>=2.3.0
flask-cors>=4.0.0